import React from 'react';
import './Header.css';

const Header = (props) => {

    return (
        <header>
            <h1>Our React Header. Probably will be on every page </h1>
        </header>
    );
};

export default Header;